using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

//[CustomEditor(typeof(DeckFactory))]
public class CastomEdit : Editor
{
 /*   public override void OnInspectorGUI()
    {
        // Отрисовываем стандартные элементы инспектора
        DrawDefaultInspector();

        // Получаем ссылку на целевой ScriptableObject
        DeckFactory ImgSpell = (DeckFactory)target;

        // Добавляем кастомную кнопку
        if (GUILayout.Button("Save Data"))
        {
            // Вызываем метод, когда кнопка нажата
            ImgSpell.RebuildDictionary();
        }
        if (GUILayout.Button("Clear Data"))
        {
            // Вызываем метод, когда кнопка нажата
            ImgSpell.ClearData();
        }
    }*/
}

